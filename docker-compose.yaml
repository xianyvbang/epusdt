services:
  epusdt:
    image: gmwallet/epusdt:latest
    restart: always
    # Reap orphaned Chromium helper processes when the browser exits.
    init: true
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./env:/app/.env
    ports:
      - "8000:8000"
